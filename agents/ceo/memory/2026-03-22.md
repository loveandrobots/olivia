# 2026-03-22 — CEO Daily Notes

## Today's Plan
- Close OLI-219 (Address Reliability) — all subtasks complete
- Check for new assignments

## Timeline

### HB1 — ~00:20 UTC
- Woke on `issue_assigned` for OLI-219
- Board approved the reliability plan ("This plan seems fine to me")
- Verified all three subtasks done:
  - OLI-220 (E2E in CI) — done
  - OLI-221 (E2E coverage expansion) — done
  - OLI-222 (Critical user paths) — done
- Closed OLI-219 as done with rollup comment
- No other assignments in inbox. Clean exit.

### HB2 — ~00:24 UTC
- Woke on `issue_reopened_via_comment` for OLI-195. Board flagged VP of Product didn't run release readiness check on latest heartbeat.
- Investigated: 15 unreleased commits on origin/main since v0.3.0, including bug fixes and error feedback feature. VP should have caught this.
- Strengthened VP HEARTBEAT.md Step 5: marked MANDATORY, added concrete git command, required check result in every heartbeat comment. Committed `29d244d`.
- Created [OLI-223](/OLI/issues/OLI-223) — immediate release assessment task assigned to VP of Product.
- Closed OLI-195.

### HB3 — ~00:50 UTC
- Woke on `issue_comment_mentioned` for OLI-207 (critical — iOS app "Can't Reach Server")
- Founding Engineer re-triaged: ATS fix confirmed in v0.4.0, not the root cause. Revised hypothesis: `IOS_API_BASE_URL` secret missing from upstream repo, so `VITE_API_BASE_URL` is empty at build time → app falls back to `localhost:3001`
- Verified code path: `ios.yml:32` reads secret, `api.ts:88` defaults to `127.0.0.1:3001`, `api.ts:94` warns in console
- Cannot access upstream repo secrets (GitHub token lacks permission)
- Escalated to board: need them to verify/set `IOS_API_BASE_URL` on `LoveAndCoding/olivia` and trigger new TestFlight build
- PR #64 (diagnostics) pending review — will surface effective API URL in Settings

### HB4 — ~06:20 UTC
- Woke on `issue_comment_mentioned` for OLI-207 again
- Significant progress since HB3: board confirmed secret is set correctly, SRE added diagnostic probe (PRs #65-#67), board provided diagnostic data showing correct URL but "Load failed"
- SRE exhausted all code-level fixes: ATS, CORS, CapacitorHttp native bypass — all in place, still failing
- SRE's conclusion: OS/network-level issue, not code. Suggests HTTPS as permanent fix
- Posted CEO direction: board to try quick diagnostics (verify v0.4.1, Tailscale reconnect, check content blockers), then move to HTTPS via Tailscale cert provisioning as permanent fix
- SRE to lead HTTPS setup once board confirms quick checks

### HB5 — ~06:27 UTC
- Woke on `issue_assigned` for OLI-228 (critical) — "Set up HTTPS via Tailscale for native app connectivity"
- Child of OLI-207. Root cause confirmed: iOS blocks cleartext HTTP from native processes. HTTPS is the permanent fix.
- Created branch `feat/oli-228-https-tailscale` from upstream/main
- Code changes:
  - `deploy/nginx.conf` — SSL server block (443) with Tailscale certs, HTTP→HTTPS redirect, HSTS header
  - `docker-compose.yml` — expose 443, mount `/etc/olivia/certs` as cert volume, switch `OLIVIA_PWA_ORIGIN` to HTTPS
  - `apps/pwa/Dockerfile` — expose 443
  - `.env.example` — updated docs for MagicDNS hostname requirement and HTTPS URLs
- Opened [PR #69](https://github.com/LoveAndCoding/olivia/pull/69) against upstream
- Moved OLI-228 to `in_review` — code ready, blocked on board infra actions:
  1. Enable HTTPS certs in Tailscale admin
  2. Provision certs on server → `/etc/olivia/certs/`
  3. Update `EC2_TAILSCALE_HOST` to MagicDNS hostname
  4. Update `IOS_API_BASE_URL` secret to HTTPS
  5. Cut new TestFlight build

### HB6 — ~07:30 UTC
- Woke on `issue_commented` for OLI-228 — board says all infra steps complete except step 5 (TestFlight build needs code changes), coordinate with VP of Product
- PR #69 still open, not merged yet
- Created [OLI-229](/OLI/issues/OLI-229) assigned to VP of Product — hotfix release + TestFlight build
- Steps: merge PR #69 → version bump → deploy → trigger TestFlight build
- This should close out the OLI-207 connectivity issue end-to-end

### HB7 — ~07:41 UTC
- Woke on `issue_assigned` for OLI-231 "Enforce Behaviors" (high priority, from board)
- Board flagged recurring problem: agents not following established workflows
- Audited all 5 agent instruction files, identified root causes
- Drafted three-layer enforcement proposal, posted as plan document on OLI-231
- Reassigned to board for review

### HB8 — ~08:00 UTC
- Woke on `issue_assigned` for OLI-231 — board approved plan with "Make it so"
- Executed Layers 1-2 of the enforcement proposal:
  - **Layer 1**: Added Hard Rules sections to all 4 agent AGENTS.md files
  - **Layer 2**: Added pre-flight checklists to all 4 agent HEARTBEAT.md files
  - Rewrote SRE Git Workflow + new Hotfix Escalation section
  - Strengthened FE version-bump rule to mandate `/version-bump` skill
- Opened [PR #71](https://github.com/LoveAndCoding/olivia/pull/71) against upstream
- Moved OLI-231 to `in_review`, assigned to board for merge
- Layer 3 (Claude Code hooks) deferred to future spike

### HB9 — ~08:06 UTC
- Woke on `issue_assigned` for OLI-236 (high) — "Configure Paperclip error reporter env vars in deployment"
- Child of OLI-233 (Errors Not Reported Automatically). SRE built the reporter but the four env vars were never added to deployment config.
- Added `OLIVIA_PAPERCLIP_*` vars to `.env.example` (with docs) and `docker-compose.yml` (passthrough)
- Opened [PR #72](https://github.com/LoveAndCoding/olivia/pull/72) against upstream
- Moved OLI-236 to `in_review`. Board still needs to set actual values in production `.env` and redeploy.
- Budget: 32% ($55.90 / $175.00)

### HB10 — ~08:15 UTC
- Woke on `issue_comment_mentioned` for OLI-233 — SRE posted triage summary with two subtasks
- OLI-236 (my subtask) already handled in HB9, PR #72 in review
- OLI-235 (frontend error reporting) actively being worked by Founding Engineer
- Inbox empty. No new work. Clean exit.

### HB11 — ~08:25 UTC
- Woke on `issue_assigned` for OLI-231 — board flagged that I broke the git workflow with PR #71
- Root cause: CEO instructions had no Hard Rules or pre-flight checklist. TOOLS.md and HEARTBEAT.md still said "target upstream" for all PRs, contradicting the board's 2026-03-21 directive that PRs to upstream are releases only.
- Fix: Added Hard Rules section to CEO AGENTS.md, rewrote HEARTBEAT.md Fork Sync + added Pre-Flight Checklist, corrected TOOLS.md references
- Committed `18a1679` to origin/main (correctly, not as upstream PR)
- Also: PRs #69, #71, #72 were all opened to upstream as per-feature PRs — all violated the workflow

### HB12 — ~14:35 UTC
- Woke on `issue_commented` for OLI-228 — board confirmed "this is complete and has resolved the root issue"
- Closed OLI-228 (done) and OLI-229 (done)
- Commented on parent OLI-207 (assigned to SRE) that root issue is resolved, can be closed
- The iOS "Can't Reach Server" saga is finally resolved: HTTPS via Tailscale fixed the cleartext HTTP block

### HB13 — ~15:15 UTC
- Woke on `issue_commented` for OLI-236 — board confirmed env vars are set, asked to ensure PRs #69/#71/#72 content is in origin/main
- PR #69 (HTTPS) was already merged to origin/main. PRs #71 and #72 had content stuck on feature branches.
- Cherry-picked OLI-236 env var changes + checked out OLI-231 agent enforcement files into main. Committed `97add77`, pushed to origin/main.
- OLI-234 (routine completion fix) also unreleased — board says routines semi-broken without it
- Created [OLI-237](/OLI/issues/OLI-237) assigned to VP of Product for v0.4.3 hotfix release
- Closed OLI-236 as done
